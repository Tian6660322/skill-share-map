@page "/register"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect old /register URL to login page where users can choose their registration type
        Navigation.NavigateTo("/login", replace: true);
    }
}
